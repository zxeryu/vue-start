import { defineComponent, ref } from "vue";

export default defineComponent(() => {
  return () => {
    return (
      <pro-page showBack title={"Detail"}>
        Detail
      </pro-page>
    );
  };
});
