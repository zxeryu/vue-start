import { defineComponent } from "vue";

import Compose from "./demo/compose";

export default defineComponent(() => {
  return () => {
    return (
      <>
        <Compose />
      </>
    );
  };
});
